import Route from '@ember/routing/route';

// by default a route model(){} returns the model of the parent route, so it does not need to be declared if that is the desired outcome
export default class BandsBandDetailsRoute extends Route {}
